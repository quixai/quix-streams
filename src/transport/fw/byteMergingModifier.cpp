#include <string>
#include <memory>
#include <sstream>
#include <functional>

#include "./byteMergingModifier.h"

namespace Quix { namespace Transport {

    using namespace std;

    void ByteMergingModifier::send(std::shared_ptr<IPackage> package) const
    {

    };

} }